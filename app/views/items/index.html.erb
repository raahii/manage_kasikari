<% provide(:title, "#{current_user.name}のアイテム一覧") %>
<h1><%="#{current_user.name}のアイテム一覧"%></h1>

<ul class="items">
  <% @items.each do |item| %>
    <li>
      <%= thumbnail_for item, size: 50 %>
      <%= link_to item.name, item %>
    </li>
  <% end %>
</ul>
