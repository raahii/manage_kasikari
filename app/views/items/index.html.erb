<% provide(:title, 'All items') %>
<h1>All items</h1>

<%= will_paginate %>

<ul class="items">
  <% @items.each do |item| %>
    <li>
      <%= thumbnail_for item, size: 50 %>
      <%= link_to item.name, item %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>

