<% if defined?(dont_link) && dont_link %>
  <li id="<%= user.id %>">
    <%= thumbnail_for user, size: 50 %>
    <%= user.name %>
  </li>
<% else %>
  <li>
    <%= thumbnail_for user, size: 50 %>
    <%= link_to user.name, user %>
  </li>
<% end %>
